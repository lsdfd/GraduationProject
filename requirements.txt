# Common dependencies shared by all environments.
# Install a matching PyTorch build separately via one of:
#   pip install -r requirements-cu124.txt
#   pip install -r requirements-cu128.txt
#   pip install -r requirements-cpu.txt

# Core scientific stack
numpy>=1.24,<3.0
scipy>=1.10,<2.0

# Logging / visualization
tensorboard>=2.14,<3.0
matplotlib>=3.7,<4.0

# RCWA
torcwa>=0.1.4
