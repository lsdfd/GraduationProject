"""Core space-time inference utilities and the main virtual experiment entrypoint.

This package intentionally exposes only the reusable ST modules under
``infer.st``. Historical one-off analyses and sample-specific reproduction
scripts live in sibling ``archive`` and ``cases`` packages and are not part of
the stable public surface.
"""
